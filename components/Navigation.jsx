import React from 'react'
import Link from 'next/link'

export default function Button({Graa}) {
  return (
    <nav href="/">
        <button className = "btn_navigation smooth_navigation">self closing tag</button>
        <h1></h1>
        <button className = "Ice_Spice">self closing tag</button>
    </nav>
  )
}